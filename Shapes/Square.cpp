#include "Square.h"

/*
void Square::draw()
{
	using namespace std;
	cout << "This is a Square with a width and height of "
		<< getWidth() << " and area of " << getArea()
		<< " located at point (" << getBL().x << ", "
		<< getBL().y << ")" << endl;
}
*/

std::string Square::getName()
{
	return std::string("Square");
}
